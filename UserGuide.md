# SynAnalyzer User Guide
This User Guide is currently under construction. The basic information required for setting up the directory needed to 
run the demo is provided below. However, additional guidance with regards to how the ImageJ macro works, the scoring process,
and how the data is compiled will be provided ASAP.

## Overview
This source code can be considered a semi-automated image analysis pipeline for quantifying the density of paired and
unpaired synaptic markers, their association with tdTomato (tdT) signal, and other properties in a given high-resolution
micrograph of a wholemount preparation of cochlear sensory epithelium. To do this it relies on the user providing a
spreadsheet that contains the xyz coordinates for every region of interest in the micrograph. For the purposes of the
Franco et al manuscript, these spreadsheets are automatically generated by Imaris from synaptic marker surface reconstructions. 
Each surface reconstruction encapsulates the fluorescence signal created by a synaptic marker punctum. Imaris calculates the
center of this reconstruction, its volume, and the mean fluorescence intensity within this surface for each channel in the
micrograph, along with several other features that were not analyzed in the Franco et al manuscript. With this xyz coordinate
and the original micrograph, the code generates a set of arrays that show a summed intensity projection thumbnail of the region of interest (i.e., centered at the given xyz coordinate), which the user then manually evaluates. Following user scoring, the code
compiles the data from the manual analysis along with the original outputs from Imaris and key experiment metadata to calculate the values presented in the manuscript. 

To achieve this, the image analysis pipeline is broken down into several steps. First, the spreadsheets generated in Imaris
are converted into Python friendly dataframes so that Pandas can be used to efficiently compute values of interest. In this step,
key image metadata is added to each entry within the dataframe so that a single surface ID can always be traced back to its
original image and to the originating fluorescence channel (i.e., whether the surface reconstruction was generated from the
ribbon or PSD immunolabelling). These dataframes are saved as new CSV files that can be easily read by ImageJ. The second step
in the pipeline is running an ImageJ macro that identifies all available micrographs and CSV files. It then opens an image-CSV
file pair and generates a set of thumbnail maximum projection images at each location in the CSV file. These thumbnails are then
compiled into an array. The macro walks the user through manually scoring each image for its "synaptic" status (i.e., whether or
not the thumbnail shows a paired ribbon and PSD). CSV files generated from PSD surface reconstructions are also scored for their "terminal" status (i.e., whether or not the PSD signal overlaps closely with tdT signal). 

Once all image-CSV file pairs have been analyzed according to the user wishes, an additional Python notebooks are used to 
complete the analysis. First, the data from all CSV files is compiled into a master "XYZSummary" dataframe. This single
dataframe has one entry for every single reconstruction analyzed along with all of its relevant metadata such as the originating
image, animal ID, experimental condition/group, etc. Within this same notebook, two new values are calculated for every surface.
An "image maximum normalized tdT intensity" is calculated using the mean tdT intensity within a synaptic PSD, as calculated by
Imaris. The code takes the brightest value in the corresponding image and normalizes the mean tdT intensity of all surfaces to 
this value and saves the information in a new column in the dataframe. Additionally, a "control median normalized volume" is
calculated by finding the median volume for the control group, across all images in that group, and then by dividing all volumes
in all images by this value. This new value is also saved as a new column in the dataframe. This Python notebook also summarizes
the data for each image (e.g., PSD-based Paired Synapse Index, tdT+ Synapse Proportion, etc.) and saves this information in 
a new CSV file called "BatchMaster." 

## Step 1: Setting up the experiment metadata
SynAnalyzer expects that image and experiment metadata is saved in two different CSV files with specific heading names. These
heading names can be altered within the Python notebooks and ImageJ macro. However, it is easiest to follow the conventions 
originally used here. These are detailed below and can be seen in the demo dataset files:
* SynAnalyzer_BatchName.Metadata.Imaging.csv (note that this is a good sheet to also track all imaging parameters used for easy reference):
  * SampleID
  * Turn (i.e., which cochlear turn was imaged)
  * RegionID (i.e., used in the case when multiple images are acquired from the same cochlear turn)
  * Frequency (i.e., the corresponding tonotopic frequency of the imaged region)
  * HairCellsReconstructed (i.e., the number of hair cells for each synaptic marker puncta were reconstructed in Imaris)
  * ImageName (note that the code expects all image filenames to follow the convention shown in the demo files and below)
    * Image naming convention: SampleID.TwoCharacterTurnID.TwoCharacterRegionID.Zs.NumberOfFluorescentChannelsC.czi
   
![Screenshot of the required imaging metadata spreadsheet](/Assets/1_MetadataScreenshots/ImagingMetadataCSV.png)

* SynAnalyzer_BatchName.Metadata.Samples.csv (note that this is a good sheet to also track animal information and any other experiment
data associated with the sample):
  * SampleID
  * AnimalID
  * Group

![Screenshot of the required sample metadata spreadsheet](/Assets/1_MetadataScreenshots/SampleMetadataCSV.png)    

## Step 1: Setting up the batch directory
Although SynAnalyzer can be adopted to analyze a single image, it is intended to run on a batch of images that are saved in 
a dedicated repository that has all other required files. This directory should be given a unique batch name, as this helps
to keep the data from unique experiments organized within a single batch. The bare minimum files required by SynAnalyzer, and
how they are expected to be organized, is shown below as it is setup for the demo data. Note that both "PreSyn" and "PostSyn"
files are not needed for the code to run. It is perfectly acceptable to run the code with just one set of surface reconstruction
data.

![Screenshot of the required initial directory organization](/Assets/1_ConvertImarisStatsScreenshots/Dependency2_DirectoryStructure.png)

## Step 2: Running the source code file 1_SynAnalyzer_ConvertImarisStatsFiles.ipynb
In this step, the files exported from Imaris (one file for each set of surface reconstructions where a single set is reconstructed 
using the fluorescence intensity of a single channel in the image) are converted into Python-friendly dataframes and key
metadata is added to each entry. Note that Imaris does sometimes alter the format of their statistics files and it may be 
necessary to alter the example code provided to capture the appropriate values. Additionally, in order for the code to 
properly convert xyz coordinates from their original values in microns to pixels, the code needs to know the voxel size of 
the corresponding images. 

### Dependencies and Inputs
The preceeding steps must be completed before this notebook can be ran successfully. Additionally, the code needs to be updated for
whatever format the Imaris Statistics Files take. 

This notebook requires the Imaris Statistics Files as inputs. An example of one of the sheets referenced in the Demo is shown below.
![Screenshot of one page in one of the Imaris Stats files in the Demo data set](/Assets/1_ConvertImarisStatsScreenshots/Dependency1_ImarisStatsFile.png)

### Outputs
After the code runs, the new XYZCSV files will be saved in a dedicated subfolder, as shown below.
![Screenshot of the batch directory after running the ConvertImarisStatsFiles notebook](/Assets/1_ConvertImarisStatsScreenshots/Output_UpdatedFiles.png)

These files can be opened in Excel. However, users are strongly urged to not edit their contents. If necessary, fresh versions can be generated
by rerunning the notebook and this will not impact any work already completed in steps 3 and 4 below. 
![Screenshot of the XYZCSV files generated in this step.](/Assets/1_ConvertImarisStatsScreenshots/Output_XYZCSV.png)

## Step 3: Running the source code file 2_SynAnalyzer_AnalyzeXYZs.ijm 
(to be completed)

## Step 4: Running the source code file 3_SynAnalyzer_CompileXYZData.ipynb
(to be completed)

## Step 5: Statistical Analysis
We recommend importing the data saved in Batch Master and XYZSummary files into Prism for statistical analysis. It is also
possible to do this with Python or R. Users are encouraged to use whichever approach is most appropriate for their applications.
Both CSV files can be easily imported into Python, R, Matlab, etc as a dataframe for plotting or statistical analysis.
